#include "syscall.h" 

int main() 
{
    int ch = 32;

    while (ch <= 126)
    {
        PrintInt2(ch);
        PrintChar(' ');
        PrintChar((char)ch);
        PrintChar('\n');
        ch++;
    }

    Halt();
}
